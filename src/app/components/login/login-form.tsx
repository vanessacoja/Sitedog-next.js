export default async function LoginForm() {
  return (
    <form action="">
      <input tupe="text" name="username" placeholder="usuario" />
      <input tupe="password" name="password" placeholder="senha" />
    <button>Entrar</button>
    </form>
  );
}
